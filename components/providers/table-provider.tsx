
import Table from "@/components/UI/Table";
import { intialJobs } from "@/lib/initial-jobs";

export const TableProvider = async() => {
	// const jobs = await intialJobs();
	// console.log('jobs: ', jobs);
    return (
        <>
			<Table
			/>
        </>
    )
}